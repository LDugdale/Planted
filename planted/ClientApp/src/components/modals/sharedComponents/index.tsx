import React from 'react';
import { ModalHeader as ModalHeaderForExport, ModalHeaderProps } from './modalHeader';

export const ModalHeader = (props: ModalHeaderProps) =>
    <ModalHeaderForExport {...props}/>